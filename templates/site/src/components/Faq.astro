---
import Section from './Section.astro'
import { FAQ } from '~/content/faq'
---

<Section id="faq" eyebrow="FAQ" title="Questions people actually ask" width="content">
  <!--
    Native <details>. An accordion component would need JavaScript, keyboard
    handling and ARIA to end up exactly here — and this one is searchable by the
    browser's find-in-page even while collapsed.
  -->
  <dl class="divide-y divide-line">
    {
      FAQ.map((item) => (
        <div class="py-1">
          <details class="group">
            <summary class="flex cursor-pointer list-none items-center justify-between gap-4 rounded-sm py-3 font-medium text-fg focus-ring marker:content-none">
              <dt>{item.question}</dt>
              <svg
                viewBox="0 0 16 16"
                width="16"
                height="16"
                aria-hidden="true"
                class="shrink-0 text-fg-subtle transition-transform group-open:rotate-45"
              >
                <path
                  d="M8 3v10M3 8h10"
                  stroke="currentColor"
                  stroke-width="1.5"
                  stroke-linecap="round"
                />
              </svg>
            </summary>
            <dd class="max-w-prose pb-4 text-sm text-fg-muted">{item.answer}</dd>
          </details>
        </div>
      ))
    }
  </dl>
</Section>
