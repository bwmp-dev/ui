# Signs and encrypts the session cookie. Generate with:
#   node -e "console.log(crypto.randomBytes(32).toString('base64url'))"
# Must be at least 32 characters. Never commit the real value.
SESSION_SECRET=development-only-secret-at-least-32-chars

# Shown in the header and the document title.
VITE_APP_NAME=Stack Fullstack
