# Base URL of the API. Leave the mock enabled to run without a backend.
VITE_API_URL=http://localhost:8080/api

# Serve every request from src/mocks instead of the network.
VITE_API_MOCK=true

# Shown in the sidebar and the document title.
VITE_APP_NAME=Stack App
