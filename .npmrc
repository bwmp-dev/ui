# Shared packages import peer deps (react, tailwind) that consumers own.
# Strict peers keeps mismatches loud instead of silently duplicating React.
strict-peer-dependencies=false
auto-install-peers=true
link-workspace-packages=true
prefer-workspace-packages=true
